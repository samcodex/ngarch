import { CommaWithSpacePipe } from './comma-with-space.pipe';

describe('CommaWithSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaWithSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
